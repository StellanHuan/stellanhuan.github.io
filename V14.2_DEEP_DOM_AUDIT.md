# V14.2 Deep DOM Audit

## V12.4-final vs HEAD — DOM Structure

### IDENTICAL (0 changes)
- Hero: <section class="hero"> → same nesting
- Quote: <section class="macro-type"> → same
- 01 Masonry: <div class="masonry" id="featuredDuo"> → same
- 02 HScroll: <div class="hscroll-wrap"> → same
- 03 Masonry: <div class="masonry" id="landscapeDuo"> → same
- About: <div class="about-section"> → same
- JS rendering: same functions, same IDs

### CHANGED (V12.5 Contact/Footer redesign)
- Contact: card → 3-column grid
- Footer: minimal → tags+links+social
- Booking: removed "点击查看作品" text
- Nav: added menu-btn <button> (hidden on desktop)

### Key finding
Masonry/Duo/About DOM 完全一致。
桌面布局无法恢复的原因不是 DOM 结构变化。

### Root cause hypothesis
1. CDN 缓存旧版 HTML
2. 浏览器窗口 < 769px 触发 @media
3. CSS .masonry 等规则被 media query 内的 !important 覆盖
   (仅在 @media 内，桌面不应受影响)
