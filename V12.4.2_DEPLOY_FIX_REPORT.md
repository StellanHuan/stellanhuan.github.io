# V12.4.2 Deploy Race Condition Fix

## Root Cause
CI triggered on  and  changes. Admin API deploys update these files, CI auto-commits in response, creating a git push race condition.

## Fix
1. Removed , ,  from CI trigger paths
2. CI now only triggers on: content/**, images/**, index.html, series/**, src/pages/**, public/**
3. Admin API deploys no longer trigger CI (no race)
4. Concurrency:  already in place

## Result
Admin deploy → GitHub API PUT → no CI trigger → no race condition → clean deploy
